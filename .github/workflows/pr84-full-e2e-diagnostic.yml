name: PR84 Sawbug stress diagnostic

on:
  push:
    branches:
      - stabilize-sawbug-self-test

permissions:
  contents: read
  issues: write

jobs:
  sawbug-stress:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run Sawbug stress and capture outcome
        id: stress
        shell: bash
        run: |
          set +e
          set -o pipefail
          pnpm exec playwright test tests/e2e/sawbug.spec.ts --repeat-each=10 --workers=2 2>&1 | tee wm-sawbug-stress.log
          code=${PIPESTATUS[0]}
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Publish diagnostic issue
        env:
          GH_TOKEN: ${{ github.token }}
          EXIT_CODE: ${{ steps.stress.outputs.exit_code }}
          SHA: ${{ github.sha }}
        shell: bash
        run: |
          if [ "$EXIT_CODE" = "0" ]; then outcome="success"; else outcome="failure"; fi
          {
            echo "Sawbug stress diagnostic on \`$SHA\`: **$outcome**"
            echo
            echo '```text'
            tail -c 50000 wm-sawbug-stress.log
            echo
            echo '```'
          } > diagnostic-body.md
          gh issue create --repo "$GITHUB_REPOSITORY" --title "[TEMP PR84] Sawbug stress diagnostic" --body-file diagnostic-body.md
